<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.example</groupId>
  <artifactId>flink-lab</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <properties>
    <flink.version>1.18.1</flink.version>
    <flink-cdc.version>3.2.1</flink-cdc.version>
  </properties>

  <dependencies>
    <!-- Flink Core -->
    <!-- [Flink Built-in] -->
    <!-- Flink DataStream API: Provides the core Java API for building and executing streaming data processing jobs. -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-streaming-java</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- [Flink Built-in] -->
    <!-- Flink Client: Provides the client APIs required to submit, configure, and execute Flink jobs. -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-clients</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- [Flink Built-in] -->
    <!-- Flink Runtime Web UI: Provides the embedded Web UI for monitoring Flink jobs in local development. -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-runtime-web</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- [Flink Built-in] -->
    <!-- Flink File Connector: Provides file system connectors for reading data from and writing data to files. -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-connector-files</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- [Flink Official Connector] -->
    <!-- Flink Kafka Connector: Provides source and sink connectors for integrating Flink with Apache Kafka. -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-connector-kafka</artifactId>
      <version>3.0.2-1.18</version>
      <scope>provided</scope>
    </dependency>

    <!-- [Flink Official Connector] -->
    <!-- Flink DataGen Connector: Provides built-in data generators for producing synthetic test and development data. -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-connector-datagen</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- [Third-Party] -->
    <!-- MySQL JDBC Driver: Provides JDBC connectivity between the application and MySQL databases. -->
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <version>8.0.33</version>
    </dependency>

    <!-- [Flink Official Connector] -->
    <!-- Flink JDBC Connector: Provides JDBC-based source and sink connectors for integrating Flink with relational databases. -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-connector-jdbc</artifactId>
      <version>3.1.2-1.18</version>
    </dependency>

    <!-- [Flink Built-in] -->
    <!-- Flink RocksDB State Backend: Provides RocksDB-based state storage for large and persistent Flink state. -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-statebackend-rocksdb</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- [Third-Party] -->
    <!-- Hadoop Client: Provides Hadoop client libraries for interacting with HDFS and other Hadoop ecosystem services. -->
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-client</artifactId>
      <version>3.3.6</version>
      <scope>provided</scope>
    </dependency>

    <!-- [Flink Built-in] -->
    <!-- Flink State Changelog: Provides incremental state change logging to improve checkpoint performance and scalability. -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-statebackend-changelog</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- [Flink Built-in] -->
    <!-- Flink Table API Java Bridge: Provides Java language support for developing Flink Table API and SQL applications. -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-table-api-java-bridge</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- [Flink Built-in] -->
    <!-- Flink Table Planner Loader: Provides the runtime components required to execute Flink Table API and SQL programs. -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-table-planner-loader</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- [Flink Built-in] -->
    <!-- Flink Table Runtime: Provides the runtime implementation required by Flink Table API and SQL jobs. -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-table-runtime</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- [Flink Ecosystem] -->
    <!-- Flink CDC MySQL Connector: Captures MySQL database changes and streams them into Flink for real-time processing. -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-connector-mysql-cdc</artifactId>
      <version>${flink-cdc.version}</version>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.5.1</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <artifactSet>
                <excludes>
                  <exclude>com.google.code.findbugs:jsr305</exclude>
                  <exclude>org.slf4j:*</exclude>
                  <exclude>log4j:*</exclude>
                </excludes>
              </artifactSet>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                  </excludes>
                </filter>
              </filters>
              <transformers combine.children="append">
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
